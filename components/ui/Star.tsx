import { useRef } from "react";
// THREE.js component disabled - not installed and not used

const StarsCanvas = () => {
  // Component disabled - THREE.js not installed
  return null;
};

export default StarsCanvas;
